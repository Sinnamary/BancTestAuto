[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short
# Couverture (config + core uniquement, pas l'interface GUI) :
#   pytest --cov=config --cov=core --cov-report=term-missing --cov-report=html
